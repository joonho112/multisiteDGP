# multisiteDGP v0.1 Internal Release Checklist

Status: prepared in Phase 11 Step 11.4 on 2026-05-09.

Scope: internal v0.1 release candidate only. This checklist is not a CRAN or
public-release submission checklist.

## Required Before Internal Distribution

- [x] Gate E external review remediation closed for internal v0.1.
- [x] DESCRIPTION version bumped to `0.1.0`.
- [x] NEWS heading and release-status note updated for `0.1.0`.
- [x] Q15 resolved: `spearman_to_pearson()` remains internal.
- [x] Q16/C14 resolved: `compute_kappa()` remains public; API ledger has 60
  surface rows.
- [x] Validation readiness caveats documented in NEWS.
- [x] Linux strict-hash provenance limitation documented.
- [x] `multisitepower` soft-Suggests/non-mainstream status documented.
- [x] pkgdown site rebuilt after NEWS/vignette/release metadata updates.
- [x] Clean install from package root verified.
- [x] Hello-world smoke workflow verified from the installed package.

## Accepted Internal v0.1 Limitations

- Linux x86_64/amd64 strict-hash artifact confirmation or rebaseline remains a
  follow-up task.
- `R CMD check --as-cran` remains an internal-v0.1 `1 NOTE` state until public
  release metadata and GitHub Pages deployment are ready.
- P3 polish items from Gate E remain post-v0.1 backlog: Quarto/Pandoc
  `--highlight-style` warning retest, test-tarball RDS preflight for public
  release, Linux print-example hash regeneration, and traceability schema
  polish.
- Traceability conflicts C01, C02, and C03 remain open governance rows outside
  the Gate E Step 11.3 target set.

## Distribution Notes

- Build source tarball from the package root with `R CMD build .`.
- Install from source or tarball in a clean R library before internal use.
- Use `inst/cookbook/R5-1-hello-world.R` as the smoke-test workflow.
