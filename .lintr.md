# `.lintr` — 설정 근거

`lint` workflow 는 위반 1 건도 허용하지 않는다 (`quit(status = 1L)`). 따라서 기본 규칙에서 벗어난 항목은 전부 근거가 있어야 한다. 이 문서가 그 근거다.

**이 파일은 `.lintr` 과 함께 유지된다.** 규칙을 바꾸면 여기도 고친다.

---

## `line_length_linter(120)`

기본값 80 대신 120. 이 패키지의 roxygen 은 `\item{...}{...}` 형태의 Rd 마크업을 쓰고, `\code{\link{...}}` 참조가 줄을 빠르게 채운다. 80 자에서는 한 항목이 서너 줄로 쪼개져 오히려 읽기 어렵다.

## `cyclocomp_linter(20)`

기본값 15 대신 20. `multisitedgp_design()` 과 `solve_trunc_gamma()` 는 fail-fast 검증 분기가 많다 — 분기 하나하나가 타입드 오류를 던지는 설계(Engineering Principle 6)의 직접적 결과이며, 쪼개면 오류 맥락이 흩어진다.

## `object_usage_linter = NULL`

비활성화. tidy evaluation (`.data$x`, `rlang::.data`)과 `test_that()` 안의 helper 참조에서 오탐이 많다.

::: 재검토 후보
이 linter 는 미사용 변수와 오타를 잡는 유용한 규칙이다. v0.3 에서 오탐 원인을 개별 `nolint` 로 처리하고 다시 켜는 것을 검토한다.
:::

## `object_name_linter` 의 `regexes`

`snake_case` 와 `dotted.case` 를 기본으로 하되, 패키지의 **도메인 표기법** 다섯 가지를 예외로 둔다. 전부 vignette 과 문서에서 일관되게 쓰이는 어휘이며, snake_case 로 바꾸면 문서가 설명하는 개념과의 연결이 끊긴다.

| 정규식 | 허용하는 것 | 근거 |
|---|---|---|
| `^[JIR]$` | `J`, `I`, `R` | 사이트 수 · informativeness · SE 비율. 논문과 blueprint 의 표준 기호 |
| `_[AB]$` | `dat_A`, `dat_B`, `des_A`, `df_B`, `kappa_A`, `sigma_tau_B`, `range_B` … | **Paradigm A**(site-size)와 **Paradigm B**(direct) 두 front door 를 대조하는 vignette 관용. `dat_a` 로 바꾸면 본문의 "Paradigm A" 와 연결이 끊긴다 |
| `^group[A-D]_` | `groupA_df`, `groupD_df` | 진단 rubric 의 네 질문 그룹 A–D (`scenario_audit()` 출력의 A/B/C/D 절) |
| `(^\|_)(I\|R\|GM)(_\|$)` | `I_grid`, `guess_I`, `I_B`, `R_B`, `GM_target`, `GM_realized`, `.preset_I_grid` | informativeness `I`, SE 비율 `R`, 기하평균 `GM`. 소문자로 쓰면 일반 변수와 구별되지 않는다 |
| `_M[1-8]$` | `design_M1`, `dat_M1` | Methodological 트랙 vignette 식별자 M1–M8 |

**적용 시점.** 2026-08-14, v0.2.0 업그레이드 Phase 6 (결함 원장 D-006). 이전에는 `^[JIR]$` 하나만 있었고 나머지 20 개 이름이 위반으로 잡혀 `lint` workflow 가 실패했다.

**대안 검토.** 이름을 전부 snake_case 로 바꾸는 방법도 있었으나, 대상이 대부분 **사용자가 읽는 vignette 코드**이고 본문 서술과 짝을 이루는 기호라 기각했다. 규약을 명문화하는 쪽이 낫다.

---

## 개별 예외 (`nolint`)

| 위치 | linter | 사유 |
|---|---|---|
| `vignettes/a1-getting-started.Rmd` 설치 청크 | `commented_code_linter` | `# install.packages("remotes")` 는 독자가 주석을 풀어 실행하도록 의도된 안내다 |

`nolint` 는 사유 주석과 함께 쓴다. 사유 없는 `nolint` 는 추가하지 않는다.
