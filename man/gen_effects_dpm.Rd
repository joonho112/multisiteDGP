% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer1-gen_effects_dpm.R
\encoding{UTF-8}
\name{gen_effects_dpm}
\alias{gen_effects_dpm}
\title{Generate Dirichlet-process-mixture latent site effects}
\usage{
gen_effects_dpm(
  J,
  tau = 0,
  sigma_tau = 0.2,
  theta_G = list(),
  formula = NULL,
  beta = NULL,
  data = NULL,
  g_fn = NULL,
  g_returns = c("standardized", "raw"),
  audit_g = TRUE
)
}
\arguments{
\item{J}{Integer. Number of sites.}

\item{tau}{Numeric. Grand mean on the response scale. Default \code{0}.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site standard deviation
on the response scale. Default \code{0.20}.}

\item{theta_G}{Named list. When a \code{g_fn} bridge is supplied, this list
is forwarded to the bridge as extra arguments (after \code{J}). Default
\code{list()}.}

\item{formula}{One-sided formula for site-level covariates, or \code{NULL}.}

\item{beta}{Numeric coefficient vector matching \code{formula}, or \code{NULL}.}

\item{data}{A \code{data.frame} with the predictors named in \code{formula}, or
\code{NULL}.}

\item{g_fn}{Optional DPM bridge callback with signature
\verb{function(J, ...)}. When \code{NULL}, the function aborts with an
install-hint pointing to the user-callback route.}

\item{g_returns}{Character. Bridge convention — \code{"standardized"}
(Convention A, default) or \code{"raw"} (Convention B). See
\code{\link{gen_effects_user}} for the contract.}

\item{audit_g}{Logical. Whether to validate (Convention A only) that
the bridge's draws meet the unit-moment contract. Default \code{TRUE}.}
}
\value{
A tibble with one row per site and columns \code{site_index},
\code{z_j}, \code{tau_j}, plus any covariate columns from \code{data}. Returned only
when a \code{g_fn} bridge is supplied; otherwise the function aborts.
}
\description{
Draw J latent site effects from a Dirichlet-process mixture. The current
release ships only the user-callback bridge for this shape — to use
\code{true_dist = "DPM"} you must supply a \code{g_fn} bridge that draws DPM
samples (typically fitted by an external package such as \pkg{dirichletprocess}
or the JAGS / Stan posterior of a DPM model). Calling without a \code{g_fn}
aborts with an install-hint pointing to the user-callback path.
}
\details{
DPM as a built-in shape is on the roadmap but currently routes through
the user-callback convention. When \code{g_fn} is supplied, this function
delegates to \code{\link{gen_effects_user}} with \code{theta_G} forwarded as
\code{g_args}. All Convention A / Convention B semantics, the audit pass, and
the cross-link to the Custom G vignette apply identically.

Typical workflow for the bridge: fit a DPM offline (e.g., via
\pkg{dirichletprocess::DirichletProcessGaussian}), wrap a finite-sample
draw in a \code{g_fn(J, ...)} closure that returns standardized residuals
(Convention A), and pass that closure to \code{sim_multisite()} /
\code{sim_meta()} via \code{g_fn} and \code{true_dist = "DPM"} (or just \code{"User"} —
the same code path).

For the formal callback contract, see the
\href{../articles/m5-custom-g-distributions.html}{Custom G distributions}
vignette.
}
\examples{
# Bridge a Gaussian DPM draw (placeholder — substitute a real DPM sampler).
gen_effects_dpm(
  J = 10L,
  g_fn = function(J) rnorm(J),
  audit_g = FALSE
)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{gen_effects_user}} for the underlying user-callback
pathway that the bridge routes through;
\code{\link{gen_effects}} for the dispatcher and the full eight-shape
catalog;
the \href{../articles/m5-custom-g-distributions.html}{M5 Custom G
distributions} vignette.

Other family-effects: 
\code{\link{gen_effects}()},
\code{\link{gen_effects_ald}()},
\code{\link{gen_effects_gaussian}()},
\code{\link{gen_effects_mixture}()},
\code{\link{gen_effects_pmslab}()},
\code{\link{gen_effects_skewn}()},
\code{\link{gen_effects_studentt}()},
\code{\link{gen_effects_user}()}
}
\concept{family-effects}
