% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_audit.R
\encoding{UTF-8}
\name{scenario_audit}
\alias{scenario_audit}
\title{Audit a grid of multisite design cells against quality-gate thresholds}
\usage{
scenario_audit(
  grid,
  M = 200L,
  thresholds = default_thresholds(),
  parallel = FALSE
)
}
\arguments{
\item{grid}{A \code{multisitedgp_design_grid} object from
\code{\link{design_grid}}.}

\item{M}{Integer (\eqn{\ge 1}). Number of simulation replicates per
cell. Default \code{200L}. Use \code{M = 50L} for a fast pre-check; raise to
\code{200L}+ for stable across-replicate medians.}

\item{thresholds}{Named list of quality-gate thresholds. Missing
entries are filled from \code{\link{default_thresholds}}.}

\item{parallel}{Logical. If \code{TRUE}, use \code{furrr::future_map_dfr()}
with the caller's active future plan. Default \code{FALSE}.}
}
\value{
A tibble with one row per design-grid cell, columns include
the cell's design parameters plus aggregated diagnostics and
pass/fail flags. \code{target_source} reports whether the Group C
distributional gates ran (\code{"auto"}) or were skipped for want of a
reference distribution (\code{"not_available"}).
}
\description{
Run \code{M} simulation replicates of each row of a \code{\link{design_grid}}
and summarize whether the cell clears the public quality-gate
thresholds. \strong{The top-of-funnel diagnostic} of Dr. Chen's four-question
rubric: run this \emph{before} committing a scenario grid to a long
simulation, so designs that cannot meet your minimum requirements
(feasibility, dependence target, distributional fit) are caught early.
}
\details{
For each cell of \code{grid}, \code{scenario_audit()} runs \code{M} replicates of
\code{\link{sim_multisite}} (or \code{\link{sim_meta}}, depending on
the cell's \code{paradigm}), computes the standard Group A/B/C/D
diagnostics on each replicate, and aggregates pass/fail status against
\code{thresholds} (defaulting to \code{\link{default_thresholds}}).

\strong{Parallel evaluation.} Set \code{parallel = TRUE} to dispatch cells
through \code{furrr::future_map_dfr()} with the caller's active future
plan. The \pkg{furrr} package is a Suggests dependency.

\strong{Recommended workflow.} (1) Build the grid with
\code{\link{design_grid}}; (2) run \code{scenario_audit(grid, M = 50L)} for
a fast pre-check; (3) drop or revise cells that fail the gates; (4)
run the long-form simulation only on the surviving cells.

For a worked example see the
\href{../articles/a6-case-study-multisite.html}{Case study — multisite
trial} vignette.
}
\section{Group C coverage}{

The Group C gates (\code{bhattacharyya}, \code{ks}) compare the realized
standardized residuals against a reference quantile function for the
declared shape. Only \code{Gaussian} and \code{StudentT} have one, so a design
declaring \code{SkewN}, \code{ALD}, \code{Mixture} or \code{PointMassSlab} is audited on
Groups A, B and D and reports \code{target_source = "not_available"}, with
\code{med_bhattacharyya}, \code{q05_bhattacharyya}, \code{med_ks} and \code{q95_ks} all
\code{NA}. The Group C gates are skipped rather than failed: an unmeasured
diagnostic is not a violated one.

\strong{This makes \code{pass} narrower than it looks.} \code{pass = TRUE} means every
gate that could run did run and passed — not that every gate ran. When
sweeping a grid that mixes shapes, read \code{target_source} alongside
\code{pass}, or the cells that skipped a whole diagnostic group will look
like the cells that cleared it.

A manual Group C check is still available for any shape: draw a
reference sample from the declared distribution and pass it as the
second argument to \code{\link{bhattacharyya_coef}} or
\code{\link{ks_distance}}.
}

\examples{
# Fast pre-check on a small grid.
grid <- design_grid(J = 10L, sigma_tau = c(0.10, 0.20), seed_root = 1L)
scenario_audit(grid, M = 1L)

\dontrun{
  # Production audit — many replicates, parallel.
  future::plan(future::multisession, workers = 4)
  scenario_audit(grid, M = 200L, parallel = TRUE)
}
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{design_grid}} for building the input scenario grid;
\code{\link{feasibility_index}} for the per-replicate Group A scalar;
\code{\link{default_thresholds}} for the default quality gates;
\code{\link{sim_multisite}} and \code{\link{sim_meta}} for the
per-replicate simulators called inside;
the \href{../articles/a3-diagnostics-in-practice.html}{A3 Diagnostics
in practice} vignette.

Other family-diagnostics:
\code{\link[=bhattacharyya_coef]{bhattacharyya_coef()}},
\code{\link[=compute_I]{compute_I()}},
\code{\link[=compute_kappa]{compute_kappa()}},
\code{\link[=compute_shrinkage]{compute_shrinkage()}},
\code{\link[=default_thresholds]{default_thresholds()}},
\code{\link[=feasibility_index]{feasibility_index()}},
\code{\link[=heterogeneity_ratio]{heterogeneity_ratio()}},
\code{\link[=informativeness]{informativeness()}},
\code{\link[=ks_distance]{ks_distance()}},
\code{\link[=mean_shrinkage]{mean_shrinkage()}},
\code{\link[=realized_rank_corr]{realized_rank_corr()}},
\code{\link[=realized_rank_corr_marginal]{realized_rank_corr_marginal()}}
}
\concept{family-diagnostics}
