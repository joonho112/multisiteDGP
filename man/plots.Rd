% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\encoding{UTF-8}
\name{plots}
\alias{plots}
\alias{plot_effects}
\alias{plot_funnel}
\alias{plot_dependence}
\title{Visualization helpers for multisiteDGP simulations}
\usage{
plot_effects(
  x,
  type = c("caterpillar", "density"),
  truth = TRUE,
  monochrome = FALSE,
  caption = TRUE,
  ...
)

plot_funnel(
  x,
  reference = c("zero", "tau"),
  envelope = TRUE,
  monochrome = FALSE,
  caption = TRUE,
  ...
)

plot_dependence(
  x,
  smoother = TRUE,
  envelope = TRUE,
  by_residual = TRUE,
  monochrome = FALSE,
  caption = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{multisitedgp_data} object from \code{\link{sim_multisite}}
or \code{\link{sim_meta}}.}

\item{type}{Character. Effect-plot view — \code{"caterpillar"} (default) or \code{"density"}.}

\item{truth}{Logical. Show latent \code{tau_j} overlay alongside observed \code{tau_j_hat}. Default \code{TRUE}.}

\item{monochrome}{Logical. Use grayscale-safe styling. Default \code{FALSE}.}

\item{caption}{Logical. Include diagnostic / provenance labels in the plot caption. Default \code{TRUE}.}

\item{...}{Reserved for future extensions.}

\item{reference}{Character. Funnel reference line — \code{"zero"} (default) or \code{"tau"} (the design grand mean).}

\item{envelope}{Logical. Show the 1.96-SE funnel envelope (the band where 95 percent of estimates would fall under no heterogeneity). Default \code{TRUE}.}

\item{smoother}{Logical. Add a loess smooth to highlight the trend. Default \code{TRUE}.}

\item{by_residual}{Logical. \code{TRUE} (default) plots residual-scale \code{z_j}; \code{FALSE} plots response-scale \code{tau_j}. Residual-scale matches the design target of Layer 3 aligners.}
}
\value{
A \code{ggplot2::ggplot} object.
}
\description{
Three plot helpers for visualizing a \code{multisitedgp_data} simulation.
Each returns a bare \code{ggplot2::ggplot} object so the caller can add
themes, labels, facets, or other layers downstream.

\describe{
\item{\code{\link{plot_effects}}}{Caterpillar (default) or density view of latent and observed site effects. Use to read off the effect-size ordering and the spread of \code{tau_j_hat} vs \code{tau_j}.}
\item{\code{\link{plot_funnel}}}{Meta-analysis funnel — \code{tau_j_hat} (x-axis) vs \code{se_j} (y-axis, inverted). Use to spot precision-effect dependence and check whether large-SE sites have the same effect distribution as small-SE sites.}
\item{\code{\link{plot_dependence}}}{Scatter of \code{z_j} (or \code{tau_j}) against \code{se2_j}. Use to verify that Layer 3 dependence alignment hit its target — the realized rank correlation should match the design.}
}

For the four-question diagnostic rubric and worked applications of
these plots, see the
\href{../articles/a3-diagnostics-in-practice.html}{A3 Diagnostics in
practice} vignette. For end-to-end case studies that use these plots,
see the \href{../articles/a6-case-study-multisite.html}{A6 multisite
trial} and \href{../articles/a7-case-study-meta-analysis.html}{A7
meta-analysis} case studies.
}
\section{Functions}{
\itemize{
\item \code{plot_effects()}: Caterpillar (default) or density view of \code{tau_j_hat} (and optionally latent \code{tau_j}). Use to read off effect-size ordering and spread.

\item \code{plot_funnel()}: Meta-analysis funnel — \code{tau_j_hat} (x-axis) vs \code{se_j} (y-axis, inverted). Use to spot precision-effect dependence and read whether large-SE sites have the same effect distribution as small-SE sites.

\item \code{plot_dependence()}: Scatter of latent effect (\code{z_j} by default, or \code{tau_j} if \code{by_residual = FALSE}) against \code{se2_j}. Use to verify that Layer 3 dependence alignment hit its target — the realized rank correlation should match the design's \code{rank_corr} (within Monte Carlo noise).

}}
\examples{
dat <- sim_multisite(J = 30L, seed = 1L)
plot_effects(dat)         # caterpillar
plot_funnel(dat)          # funnel
plot_dependence(dat)      # z_j vs se2_j scatter
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{realized_rank_corr}}, \code{\link{informativeness}},
\code{\link{heterogeneity_ratio}} for scalar diagnostics that
complement the visualizations;
the \href{../articles/a3-diagnostics-in-practice.html}{A3 Diagnostics
in practice} vignette.
}
\concept{family-plots}
