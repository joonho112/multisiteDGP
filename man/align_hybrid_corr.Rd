% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer3-align_hybrid_corr.R
\encoding{UTF-8}
\name{align_hybrid_corr}
\alias{align_hybrid_corr}
\title{Align precision ranks using hybrid copula initialization and rank polishing}
\usage{
align_hybrid_corr(
  upstream,
  rank_corr = 0,
  init = c("copula", "rank"),
  polish = c("hill_climb", "none"),
  max_iter = 20000L,
  tol = 0.02,
  dependence_fn = NULL,
  ...
)
}
\arguments{
\item{upstream}{A Layer 2 data frame with canonical columns
\verb{site_index, z_j, tau_j, se_j, se2_j, n_j}.}

\item{rank_corr}{Numeric in \verb{[-1, 1]}. Target residual Spearman
correlation. Default \code{0}. Typical applied values: \code{0.3} (moderate
positive), \code{-0.5} (selection-on-precision in meta-analysis).}

\item{init}{Character. Initialization mode — \code{"copula"} (default,
recommended) or \code{"rank"} (partial-sort start).}

\item{polish}{Character. Polishing mode — \code{"hill_climb"} (default,
recommended) or \code{"none"} (copula-only).}

\item{max_iter}{Integer (\eqn{\ge 100}). Maximum swap proposals during
the polish stage. Default \code{20000L}.}

\item{tol}{Numeric (> 0). Absolute tolerance for the realized residual
Spearman. Default \code{0.02}.}

\item{dependence_fn}{Optional callback. See \code{\link{align_rank_corr}}
for the contract.}

\item{...}{Additional arguments forwarded to \code{dependence_fn}.}
}
\value{
The upstream tibble with paired precision columns permuted, plus
attributes \code{permutation_perm} and \code{dependence_diagnostics} (with
stage-specific entries: \code{init_method}, \code{polish_method}, \code{init_realized},
\code{polish_realized}).
}
\description{
The recommended default Layer 3 dependence injector. \code{align_hybrid_corr()}
combines fast empirical-rank Gaussian-copula initialization with an
optional hill-climb polish, getting the best of both worlds: the smooth
joint distribution of the copula path and the tight target-realized
agreement of the hill-climb. It preserves the empirical precision
multiset exactly via explicit permutation.
}
\details{
\strong{Two-stage algorithm.} Stage 1 — initialization. By default,
run \code{\link{align_copula_corr}} with \code{pearson_corr} chosen so the
implied Spearman matches \code{rank_corr}. (Alternative: \code{init = "rank"}
starts from a partial-sort permutation seeded by ordered indexing.)
Stage 2 — polish. By default, run a hill-climb on the Stage 1 output
until the realized Spearman lands within \code{tol} of \code{rank_corr} or
\code{max_iter} swaps have been proposed. Stage 2 can be skipped with
\code{polish = "none"} for a copula-only result.

\strong{Why it's the default.} Pure copula (Stage 1 only) is fast but
has finite-J slack on the realized Spearman; pure hill-climb is exact
but slow to converge from an arbitrary starting permutation, especially
near \eqn{|rank_{corr}| \to 1}. The hybrid uses copula to land in the
right neighborhood and hill-climb to clean up — typically converges in
far fewer swaps than pure rank, with comparable target accuracy.

\strong{Custom \code{dependence_fn} extensibility.} See the same contract
documented in \code{\link{align_rank_corr}}.

For the formal contrast among the three injection methods and a
decision rubric, see the
\href{../articles/m4-precision-dependence-theory.html}{Precision
dependence — three injection methods} vignette.
}
\section{RNG policy}{

Stage 1 (copula init) consumes one \code{rnorm()} draw per site at finite
\code{pearson_corr}. Stage 2 (hill-climb polish) proposes swaps via
\code{sample.int()} and accepts deterministically; consumes the active
\code{sample.int()} RNG. Both stages are wrapped in the caller's seed when
invoked through \code{\link{sim_multisite}} / \code{\link{sim_meta}}.
}

\examples{
# Recommended default: hybrid copula + hill-climb polish.
effects <- gen_effects_gaussian(J = 12L)
margins <- gen_site_sizes(effects, J = 12L, nj_mean = 40, cv = 0.2)
aligned <- align_hybrid_corr(margins, rank_corr = 0.3, max_iter = 1000L)
cor(aligned$z_j, aligned$se2_j, method = "spearman")

# Copula-only (skip the polish stage).
copula_only <- align_hybrid_corr(margins, rank_corr = 0.3, polish = "none")
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{align_rank_corr}} for the pure hill-climb;
\code{\link{align_copula_corr}} for the pure copula path;
the \href{../articles/m4-precision-dependence-theory.html}{M4
Precision dependence theory} vignette.

Other family-dependence: 
\code{\link{align_copula_corr}()},
\code{\link{align_rank_corr}()}
}
\concept{family-dependence}
