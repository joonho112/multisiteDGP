% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters.R
\encoding{UTF-8}
\name{adapters}
\alias{adapters}
\alias{as_metafor}
\alias{as_baggr}
\alias{as_multisitepower}
\title{Adapt multisiteDGP output for downstream meta-analysis and power packages}
\usage{
as_metafor(x, ...)

as_baggr(x, ..., include_truth = FALSE)

as_multisitepower(x, ...)
}
\arguments{
\item{x}{A \code{multisitedgp_data} object from \code{\link{sim_multisite}}
or \code{\link{sim_meta}}.}

\item{...}{Reserved for future extensions.}

\item{include_truth}{Logical. When \code{TRUE}, includes the latent true effects as a \code{tau_true} column — useful for diagnostic plots that compare baggr's posterior to the simulation truth. Default \code{FALSE}.}
}
\value{
A plain \code{tbl_df} with renamed columns. No multisiteDGP class or
attributes are carried through — the result is suitable to pass
directly to the downstream package's API.
}
\description{
Strip the multisiteDGP attributes and class from a simulated dataset and
rename the canonical columns to the conventions of a downstream
analysis package. The numeric values themselves are NEVER transformed —
only the column names change. Three adapters ship with the package, each
targeting a soft-dependency analysis package:
\describe{
\item{\code{\link{as_metafor}}}{Renames to \pkg{metafor}'s \verb{(yi, vi, sei)} convention. Use as input to \code{metafor::rma()} or \code{metafor::escalc()}.}
\item{\code{\link{as_baggr}}}{Renames to \pkg{baggr}'s \verb{(tau, se)} convention; optional \code{tau_true} column for diagnostic plotting.}
\item{\code{\link{as_multisitepower}}}{Renames to \pkg{multisitepower}'s \verb{(site, estimate, se, n)} convention. Use for empirical-Bayes power analysis.}
}
}
\details{
\strong{Soft-dependency guard.} Each adapter checks that the target
package is installed and aborts with a friendly install hint if not.
\pkg{metafor}, \pkg{baggr}, and \pkg{multisitepower} are Suggests, not
Imports — they are not required to use multisiteDGP itself.

\strong{Reserved column-name protection.} If your simulation includes
covariate columns that collide with the target package's reserved
names (e.g., a covariate named \code{yi} would collide with metafor), the
adapter aborts and asks you to rename the offending column first. This
prevents silent overwrite.

\strong{Covariate pass-through.} Any non-canonical columns in the
upstream \code{multisitedgp_data} (covariates from \code{formula} /
\code{beta} / \code{data}) are carried through to the adapted tibble after the
renamed canonical columns.

For a full workflow walkthrough see the
\href{../articles/m6-adapters-downstream.html}{Adapters and downstream
packages} vignette.
}
\section{Functions}{
\itemize{
\item \code{as_metafor()}: Convert to \pkg{metafor}'s \verb{(yi, vi, sei)} column convention. \code{yi <- tau_j_hat}, \code{vi <- se2_j}, \code{sei <- se_j}. Pass the result directly to \code{metafor::rma()} or \code{metafor::escalc()}. Soft-dep guard on \pkg{metafor}.

\item \code{as_baggr()}: Convert to \pkg{baggr}'s \verb{(tau, se)} column convention. \code{tau <- tau_j_hat}, \code{se <- se_j}. Use as input to \code{baggr::baggr()}. Soft-dep guard on \pkg{baggr}.

\item \code{as_multisitepower()}: Convert to \pkg{multisitepower}'s \verb{(site, estimate, se, n)} column convention. \code{site <- site_index}, \code{estimate <- tau_j_hat}, \code{se <- se_j}, \code{n <- n_j} (when present). Use as input to \code{multisitepower::power_calc()}. Soft-dep guard on \pkg{multisitepower}.

}}
\examples{
dat <- sim_multisite(J = 10L, seed = 1L)

# metafor: yi = tau_hat, vi = se2, sei = se.
if (requireNamespace("metafor", quietly = TRUE)) {
  md <- as_metafor(dat)
  metafor::rma(yi = yi, vi = vi, data = md)
}

# baggr: tau = tau_hat, se = se. Add latent truth for diagnostic plots.
if (requireNamespace("baggr", quietly = TRUE)) {
  bg <- as_baggr(dat, include_truth = TRUE)
}
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{sim_multisite}} and \code{\link{sim_meta}} for the
upstream simulators;
the \href{../articles/m6-adapters-downstream.html}{M6 Adapters and
downstream packages} vignette.
}
\concept{family-adapters}
