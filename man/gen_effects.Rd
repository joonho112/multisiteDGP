% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer1-gen_effects.R
\encoding{UTF-8}
\name{gen_effects}
\alias{gen_effects}
\title{Generate latent site effects}
\usage{
gen_effects(
  J,
  true_dist = c("Gaussian", "StudentT", "SkewN", "ALD", "Mixture", "PointMassSlab",
    "User", "DPM"),
  tau = 0,
  sigma_tau = 0.2,
  variance = 1,
  theta_G = list(),
  formula = NULL,
  beta = NULL,
  data = NULL,
  g_fn = NULL,
  g_returns = c("standardized", "raw"),
  audit_g = TRUE,
  upstream = NULL
)
}
\arguments{
\item{J}{Integer. Number of sites.}

\item{true_dist}{Character. One of \code{"Gaussian"}, \code{"StudentT"}, \code{"SkewN"},
\code{"ALD"}, \code{"Mixture"}, \code{"PointMassSlab"}, \code{"User"}, or \code{"DPM"}. Default
\code{"Gaussian"}. If \code{g_fn} is supplied without \code{true_dist}, the package
auto-selects \code{"User"}.}

\item{tau}{Numeric. Grand mean on the response scale. Default \code{0}.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site standard deviation
on the response scale (not variance). Default \code{0.20}.}

\item{variance}{Numeric. Legacy Gaussian variance argument. Default \code{1}.
The unit-variance convention requires \code{variance = 1}; other shapes
ignore it.}

\item{theta_G}{Named list of shape-specific parameters. Keys vary by
\code{true_dist}; see the eight-shape catalog above.}

\item{formula}{One-sided formula for site-level covariates (e.g.,
\code{~ x1 + x2}), or \code{NULL}.}

\item{beta}{Numeric coefficient vector matching the columns of the model
matrix built from \code{formula}, or \code{NULL}.}

\item{data}{A \code{data.frame} containing the predictors named in \code{formula},
or \code{NULL}.}

\item{g_fn}{Optional user callback for \code{true_dist = "User"} (or for the
\code{"DPM"} bridge). Receives \code{J} and returns a length-\code{J} numeric vector.}

\item{g_returns}{Character. \code{"standardized"} (Convention A, default) — the
callback returns standardized residuals \eqn{z_j} and the package
rescales. \code{"raw"} (Convention B) — the callback returns response-scale
effects and the package does not rescale.}

\item{audit_g}{Logical. When \code{g_returns = "standardized"}, validate that
the callback draws meet the unit-moment contract. Default \code{TRUE}. Has
no effect under \code{g_returns = "raw"}.}

\item{upstream}{Reserved for future layer composition. Leave \code{NULL} (the
default); passing a non-\code{NULL} value aborts.}
}
\value{
A tibble with one row per site:
\describe{
\item{\code{site_index}}{Integer 1..J — preserved through downstream layers.}
\item{\code{z_j}}{Standardized residual effect — mean 0, variance 1 by construction.}
\item{\code{tau_j}}{Response-scale latent effect, \eqn{\tau + X_j\boldsymbol{\beta} + \sigma_\tau\,z_j}.}
\item{\verb{<covariate columns>}}{Pass-through from \code{data} if \code{formula} was non-\code{NULL}.}
\item{\code{latent_component}}{Character; for \code{true_dist = "Mixture"}, names which mixture component each row was drawn from. Absent for the other seven shapes.}
}
The tibble carries no S3 class beyond \code{tbl_df} — Layer 2 functions add
the package's classes on top.
}
\description{
\code{gen_effects()} is the Layer 1 entry point of the multisiteDGP pipeline —
it draws standardized site effects \eqn{z_j} from one of eight built-in
\eqn{G} distributions (or a user callback) and returns a forward-compatible
tibble that Layers 2 through 4 can consume. Most users invoke it indirectly
through \code{\link{sim_multisite}} or \code{\link{sim_meta}}; call it
directly when composing the four layers manually or auditing a single
layer in isolation. Shape selection is controlled by \code{true_dist},
shape-specific parameters travel through \code{theta_G}, and a user callback
\code{g_fn} overrides the catalog when \code{true_dist = "User"}.
}
\details{
The eight built-in \eqn{G} distributions are:
\describe{
\item{\code{"Gaussian"} (\code{\link{gen_effects_gaussian}})}{Standard normal — the canonical baseline. No \code{theta_G} keys.}
\item{\code{"StudentT"} (\code{\link{gen_effects_studentt}})}{Standardized Student-\eqn{t} with degrees of freedom \code{theta_G$nu} (numeric, > 2). Heavier tails than Gaussian.}
\item{\code{"SkewN"} (\code{\link{gen_effects_skewn}})}{Standardized skew-normal with slant \code{theta_G$slant} (numeric). Asymmetric shape.}
\item{\code{"ALD"} (\code{\link{gen_effects_ald}})}{Standardized asymmetric Laplace with asymmetry \code{theta_G$rho} \eqn{\in (0, 1)}.}
\item{\code{"Mixture"} (\code{\link{gen_effects_mixture}})}{Two-component normal mixture with \code{theta_G$delta} (component separation), \code{theta_G$eps} (mixing weight), \code{theta_G$ups} (variance ratio). Use for bimodal or contaminated effects.}
\item{\code{"PointMassSlab"} (\code{\link{gen_effects_pmslab}})}{Point mass at 0 with probability \code{theta_G$pi0}, plus a continuous slab governed by \code{theta_G$slab_shape}, \code{theta_G$mu_slab}, \code{theta_G$sigma_slab}. Use when a fraction of sites have null effects.}
\item{\code{"User"} (\code{\link{gen_effects_user}})}{Any user callback \code{g_fn} returning length-\code{J} standardized residuals (or raw response-scale effects under \code{g_returns = "raw"}).}
\item{\code{"DPM"} (\code{\link{gen_effects_dpm}})}{Dirichlet-process mixture — currently available only via the \code{g_fn} callback bridge. Direct DPM is unimplemented in the current release.}
}

\strong{When to call this directly.} For most users,
\code{\link{sim_multisite}} or \code{\link{sim_meta}} is the right entry
point — direct calls to \code{gen_effects()} are an advanced surface. The three
situations that warrant a direct call are: composing the four layers
manually to inspect or modify the Layer 1 → Layer 2 contract; auditing a
suspected downstream diagnostic by verifying Layer 1 in isolation; and
testing a \code{g_fn} callback's output before plugging it into the full
simulation.

\strong{Unit-variance convention.} All eight shapes share a unit-variance
standardization: the package draws \eqn{z_j} with \eqn{E[z_j] = 0} and
\eqn{\mathrm{Var}(z_j) = 1}, then rescales to
\eqn{\tau_j = \tau + X_j\boldsymbol{\beta} + \sigma_\tau\,z_j}. This makes
\code{sigma_tau} a single comparable knob across shapes — heterogeneity targets
mean the same thing whether \code{true_dist = "Gaussian"} or
\code{true_dist = "ALD"}.

\strong{Convention A vs Convention B (user callbacks).} Under
\code{g_returns = "standardized"} (Convention A, the default) the callback
returns standardized residuals \eqn{z_j}; the package rescales by
\code{sigma_tau} and adds \code{tau} (and any covariate adjustment) to form \code{tau_j}.
Under \code{g_returns = "raw"} (Convention B) the callback returns the
response-scale effect directly; the package leaves it untouched.
Convention A integrates with downstream diagnostics (notably
\code{\link{informativeness}} and \code{\link{heterogeneity_ratio}})
without further work; Convention B is for callbacks where standardization
is meaningless or undesirable. See \code{\link{gen_effects_user}}.

\strong{Covariate adjustment.} When \code{formula} is non-\code{NULL}, a model
matrix \eqn{X} is built from \code{data} and combined with \code{beta} to form
\eqn{X_j\boldsymbol{\beta}}, which enters the linear predictor for
\eqn{\tau_j} additively. The covariate columns from \code{data} pass through
to the returned tibble so downstream layers can recover them.

For per-shape derivations and decision rubrics, see the
\href{../articles/m2-g-distribution-catalog.html}{G-distribution catalog
and standardization} vignette. For the \code{g_fn} callback contract, see the
\href{../articles/m5-custom-g-distributions.html}{Custom G distributions}
vignette. For the \code{formula} / \code{beta} / \code{data} covariate surface, see the
\href{../articles/a4-covariates-precision-dependence.html}{Covariates and
precision dependence} vignette.
}
\examples{
# Gaussian (default — the canonical baseline).
gauss <- gen_effects(J = 10L, true_dist = "Gaussian", sigma_tau = 0.2)
head(gauss)

# Student-t with df = 5 — heavier tails for a robustness check.
studentt <- gen_effects(J = 50L, true_dist = "StudentT",
                        sigma_tau = 0.2, theta_G = list(nu = 5))

# Mixture: two-component bimodal effects.
mix <- gen_effects(J = 50L, true_dist = "Mixture",
                   sigma_tau = 0.2,
                   theta_G = list(delta = 1.0, eps = 0.2, ups = 2.0))
table(mix$latent_component)

# Covariate-adjusted: tau_j = tau + 0.3 * x_j + sigma_tau * z_j.
sites <- data.frame(x = rnorm(20))
cov <- gen_effects(J = 20L, true_dist = "Gaussian",
                   formula = ~ x, beta = 0.3, data = sites,
                   sigma_tau = 0.15)

# User callback (Convention A — standardized residuals).
my_g <- function(J) rnorm(J)
user <- gen_effects(J = 50L, g_fn = my_g)  # auto-selects true_dist = "User"
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
Shape-specific generators: \code{\link{gen_effects_gaussian}},
\code{\link{gen_effects_studentt}}, \code{\link{gen_effects_skewn}},
\code{\link{gen_effects_ald}}, \code{\link{gen_effects_mixture}},
\code{\link{gen_effects_pmslab}}, \code{\link{gen_effects_user}},
\code{\link{gen_effects_dpm}}.
Wrappers that compose all four layers: \code{\link{sim_multisite}},
\code{\link{sim_meta}}.
The \href{../articles/m2-g-distribution-catalog.html}{M2 G-distribution
catalog} and
\href{../articles/m5-custom-g-distributions.html}{M5 Custom G
distributions} vignettes.

Other family-effects: 
\code{\link{gen_effects_ald}()},
\code{\link{gen_effects_dpm}()},
\code{\link{gen_effects_gaussian}()},
\code{\link{gen_effects_mixture}()},
\code{\link{gen_effects_pmslab}()},
\code{\link{gen_effects_skewn}()},
\code{\link{gen_effects_studentt}()},
\code{\link{gen_effects_user}()}
}
\concept{family-effects}
