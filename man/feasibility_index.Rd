% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer2-diagnostics.R
\encoding{UTF-8}
\name{feasibility_index}
\alias{feasibility_index}
\title{Compute the additive feasibility index (Efron or Morris)}
\usage{
feasibility_index(
  se2_j,
  sigma_tau = NULL,
  kind = c("efron", "morris"),
  warn = TRUE
)
}
\arguments{
\item{se2_j}{A positive numeric \eqn{\widehat{se}_j^2} vector, or a
\code{multisitedgp_data} object whose \code{se2_j} column will be used.}

\item{sigma_tau}{Numeric scalar (\eqn{\ge 0}). Between-site SD.
Required when \code{se2_j} is numeric; optional when \code{se2_j} is a data
object (auto-read from the attached design when \code{NULL}). Default
\code{NULL}.}

\item{kind}{Character. Feasibility convention. \code{"efron"} (default)
returns \eqn{\sum_j S_j}; \code{"morris"} returns
\eqn{\sum_j (1 - S_j)}.}

\item{warn}{Logical. If \code{TRUE} (default), emit a warning when the
Efron statistic lands in the FAIL band defined by
\code{default_thresholds()$feasibility_min}. Ignored for
\code{kind = "morris"}.}
}
\value{
A scalar double. For \code{"efron"}, in \verb{[0, J]}. For \code{"morris"},
in \verb{[0, J]} complementary.
}
\description{
Compute one of two additive feasibility statistics built on the
per-site shrinkage
\eqn{S_j = \sigma_\tau^2 / (\sigma_\tau^2 + \widehat{se}_j^2)}:
the \strong{Efron} form \eqn{\sum_{j=1}^{J} S_j} (total effective
information; default) or the \strong{Morris} form
\eqn{\sum_{j=1}^{J} (1 - S_j)} (total residual variation).
\strong{Group A diagnostic} (top-of-funnel feasibility — Dr. Chen's
question 1: "Is there enough effective site-level information for
partial pooling to do real work?"). Run this \emph{before} committing a
long simulation to disk.
}
\details{
\strong{Two conventions.} \code{kind = "efron"} returns \eqn{\sum_j S_j},
interpretable as "the equivalent number of perfectly-informative
sites": a design with \eqn{J = 50} and \eqn{\sum_j S_j = 8} has the
effective information of about eight noise-free sites.
\code{kind = "morris"} returns \eqn{\sum_j (1 - S_j)}, the
residual-variation form used in Morris-style empirical-Bayes
diagnostics. The two are linked by
\eqn{\sum_j S_j + \sum_j (1 - S_j) = J}.

\strong{Reading guide.} The canonical quality gate is the Efron form
\eqn{\sum_j S_j \ge 5} (see
\code{default_thresholds()$feasibility_min = 5.0}). Designs that fall
below this threshold are unlikely to support partial-pooling
estimators well — heterogeneity cannot be reliably recovered from
the available site-level information. With \code{warn = TRUE} (default),
the function emits a warning when the Efron statistic crosses into
the FAIL band.

\strong{Method dispatch.} When \code{se2_j} is a \code{multisitedgp_data}
object, \code{sigma_tau} is auto-read from
\code{attr(se2_j, "design")$sigma_tau} if not supplied; the function then
operates on \code{se2_j$se2_j}. When \code{se2_j} is a bare numeric vector,
\code{sigma_tau} is required.

For how feasibility interacts with the other Group A/B/C/D gates see
the \href{../articles/a3-diagnostics-in-practice.html}{A3 \enc{·}{.}
Diagnostics in practice} vignette.
}
\examples{
dat <- sim_multisite(J = 10L, seed = 1L)

# Efron form (default): sum of S_j; equivalent number of perfectly
# informative sites.
feasibility_index(dat, warn = FALSE)

# Morris form: sum of (1 - S_j); total residual variation.
feasibility_index(dat, kind = "morris", warn = FALSE)

# The two conventions sum to J.
feasibility_index(dat, kind = "efron", warn = FALSE) +
  feasibility_index(dat, kind = "morris", warn = FALSE)

# Numeric path: pass se2_j and sigma_tau directly.
feasibility_index(dat$se2_j, sigma_tau = 0.20, warn = FALSE)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{compute_shrinkage}} for the per-site \eqn{S_j} the
index sums;
\code{\link{informativeness}} for the geometric-mean Group A
companion;
\code{\link{mean_shrinkage}} for the Group D mean of \eqn{S_j};
\code{\link{default_thresholds}} for the \code{feasibility_min} gate;
\code{\link{scenario_audit}} for the audit pipeline;
the \href{../articles/a3-diagnostics-in-practice.html}{A3 \enc{·}{.}
Diagnostics in practice} vignette.

Other family-diagnostics: 
\code{\link{bhattacharyya_coef}()},
\code{\link{compute_I}()},
\code{\link{compute_kappa}()},
\code{\link{compute_shrinkage}()},
\code{\link{default_thresholds}()},
\code{\link{heterogeneity_ratio}()},
\code{\link{informativeness}()},
\code{\link{ks_distance}()},
\code{\link{mean_shrinkage}()},
\code{\link{realized_rank_corr}()},
\code{\link{realized_rank_corr_marginal}()},
\code{\link{scenario_audit}()}
}
\concept{family-diagnostics}
