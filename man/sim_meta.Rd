% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper-sim_meta.R
\encoding{UTF-8}
\name{sim_meta}
\alias{sim_meta}
\title{Simulate a direct-precision meta-analysis data-generating process}
\usage{
sim_meta(design = NULL, ..., seed = NULL)
}
\arguments{
\item{design}{Optional \code{multisitedgp_design} with \code{paradigm = "direct"}.
If \code{NULL}, \code{...} is forwarded to \code{\link{multisitedgp_design}} with
\code{paradigm = "direct"} automatically locked. Construct a design once
when reusing across multiple \code{sim_meta()} calls or a
\code{\link{design_grid}} sweep.}

\item{...}{Flat direct-precision design arguments used only when
\code{design = NULL}. All must be named (positional \code{...} is rejected). The
key direct-path arguments are \code{J} (number of studies), \code{I} (mean
informativeness target, \eqn{0 < I < 1}), \code{R} (heterogeneity ratio
target, \eqn{R \ge 1}), \code{tau} (grand mean), \code{sigma_tau} (between-study
SD), \code{true_dist} (one of \code{"Gaussian"}, \code{"StudentT"}, \code{"SkewN"}, \code{"ALD"},
\code{"Mixture"}, \code{"PointMassSlab"}, \code{"User"}, \code{"DPM"}), \code{shuffle},
\code{dependence}, \code{se_fn}, \code{se_args}. See \code{\link{multisitedgp_design}}
for the full list. Passing \code{paradigm} is an error (the wrapper locks
it). Site-size arguments (\code{nj_mean}, \code{cv}, \code{engine}, \code{n_per_site})
trigger a coherence error pointing to \code{\link{sim_multisite}}.}

\item{seed}{Optional integer seed override. When supplied, replaces
\code{design$seed} and gives bit-identical reruns. Use a small integer (e.g.
\code{1L}) for examples; use a 9-digit integer in production for cross-run
uniqueness.}
}
\value{
A \code{multisitedgp_meta} tibble (which inherits from
\code{multisitedgp_data}) with one row per study and columns:
\describe{
\item{\code{site_index}}{Integer study identifier \eqn{j = 1, \ldots, J}.}
\item{\code{z_j}}{Standardized residual effect (mean 0, variance 1).}
\item{\code{tau_j}}{Latent study-level effect on the response scale, \eqn{\tau + \sigma_\tau\,z_j}.}
\item{\code{tau_j_hat}}{Observed study-level estimate \eqn{\widehat{\tau}_j}.}
\item{\code{se_j}, \code{se2_j}}{Study-level SE and sampling variance \eqn{\widehat{se}_j^2} from the direct grid (or \code{se_fn} output).}
\item{\code{n_j}}{Always \code{NA_integer_} — the direct-precision path has no site-size margin.}
}
Plus the following attributes:
\describe{
\item{\code{design}}{The locked \code{multisitedgp_design} with \code{paradigm = "direct"}.}
\item{\code{diagnostics}}{Group A / B / C / D diagnostics — \code{I_hat}, \code{R_hat}, realized Spearman / Pearson correlations, plus the meta-specific extras \code{target_I}, \code{target_R}, \code{I_error}, \code{R_error} (zero under the deterministic grid; \code{NA_real_} under custom \code{se_fn}), \code{I_exact}, \code{R_exact} (logical exactness flags), \code{shuffle}, \code{direct_se_method} (\code{"grid"} or \code{"custom"}), \code{direct_se_diagnostics}, \code{margin_engine}. See \code{\link{compute_I}}.}
\item{\code{provenance}}{Package version, R version, platform, resolved seed, \code{canonical_hash}, \code{design_hash}, and the call expression.}
\item{\code{multisitedgp_version}, \code{paradigm}}{Convenience copies for quick attribute lookup.}
}
}
\description{
\code{sim_meta()} is the unified interface for the four-layer multisite-trial
data-generating process under direct precision-scale targets. Given a
meta-analytic design — site count \eqn{J}, latent-effect distribution,
mean informativeness
\eqn{I = \sigma_\tau^2 / (\sigma_\tau^2 + \mathrm{GM}(\widehat{se}_j^2))},
and heterogeneity ratio \eqn{R = \max \widehat{se}^2 / \min \widehat{se}^2}
— it composes the latent-effects, direct-precision-margin,
dependence-alignment, and observation layers in one call and returns a
\code{multisitedgp_meta} tibble with \code{diagnostics} and \code{provenance}
attributes; the canonical hash is stored at
\code{attr(x, "provenance")$canonical_hash} (not as a top-level attribute).
\code{\link{sim_multisite}} is the sister \emph{site-size-driven}
(Paradigm A) interface; \code{sim_meta()} covers the \emph{direct-precision}
(Paradigm B) front door, where precision targets are supplied directly
rather than derived from per-site sample sizes.
}
\details{
The simulation runs four generative layers in order:
\describe{
\item{\strong{Layer 1 — latent effects} (\code{\link{gen_effects}})}{Draws standardized site effects \eqn{z_j} from one of eight built-in \eqn{G} distributions and rescales to \eqn{\tau_j = \tau + \sigma_\tau\,z_j}.}
\item{\strong{Layer 2 — direct precision margin} (\code{\link{gen_se_direct}})}{Builds the per-site sampling variance \eqn{\widehat{se}_j^2} as a deterministic grid that exactly hits the user-specified \eqn{(I, R)} targets. The site-size column \eqn{n_j} is \code{NA_integer_} because no site-size margin is involved.}
\item{\strong{Layer 3 — precision dependence} (\code{\link{align_rank_corr}}, \code{\link{align_copula_corr}}, \code{\link{align_hybrid_corr}})}{Optionally aligns \eqn{\widehat{se}_j^2} against \eqn{\tau_j} to a target Spearman correlation, preserving both marginals exactly.}
\item{\strong{Layer 4 — observation draws} (\code{\link{gen_observations}})}{Draws \eqn{\widehat{\tau}_j \sim \mathcal{N}(\tau_j,\, \widehat{se}_j^2)}.}
}

The direct-precision path is the formal counterpart of the site-size-driven
path covered by \code{\link{sim_multisite}}: where the site-size path
derives precision from sample sizes \eqn{n_j}, the direct-precision path
supplies the precision targets and the package generates a deterministic
SE grid to match exactly. Set \code{shuffle = TRUE} to randomize the SE-to-site
assignment within the wrapped seed; the multiset of \eqn{\widehat{se}_j^2}
values is preserved, so the \verb{(I, R)} targets remain exact under
permutation but the assignment to sites changes. Supply a custom \code{se_fn}
to plug in a non-grid SE distribution; when \code{se_fn} is non-\code{NULL} the
deterministic-grid invariant no longer applies and the diagnostic
\code{I_error} / \code{R_error} slots become \code{NA_real_}.

Site-size arguments (\code{nj_mean}, \code{cv}, \code{nj_min}, \code{engine}, \code{n_per_site})
are rejected with a coherence error pointing the caller to
\code{\link{sim_multisite}}; they belong to Paradigm A, not the
direct-precision path.

For a workflow walkthrough see the
\href{../articles/a7-case-study-meta-analysis.html}{meta-analysis case
study} vignette. For the formal contract on direct-precision SE
generation, see \href{../articles/m3-margin-se-models.html}{Margin and SE
models — site-size and direct-precision paths}.
}
\section{RNG policy}{

If \code{seed} is \code{NULL}, the pipeline runs under the caller's active RNG state.
If \code{seed} is a single integer, the full pipeline is wrapped in
\code{\link[withr]{with_seed}}, so the caller's global RNG state is
restored on exit. The direct SE grid is deterministic except for
\code{shuffle = TRUE} with \code{R > 1}, which uses the active sample RNG inside
the same wrapper seed.
}

\examples{
# Minimal: a defensible meta-analysis preset, one call, read realized I.
dat <- sim_meta(design = preset_meta_modest(), seed = 1L)
attr(dat, "diagnostics")$I_hat

# Explicit direct-precision design via flat (J, I, R) targets.
dat <- sim_meta(J = 12L, I = 0.30, R = 2, sigma_tau = 0.20, seed = 1L)
summary(dat)

# The grid generator is exact: I_error and R_error are zero.
diag <- attr(dat, "diagnostics")
diag$I_error  # 0
diag$R_error  # 0

\dontrun{
  # Hand off to a meta-analytic estimator (requires {metafor}).
  # `as_metafor()` renames the canonical columns to metafor's (yi, vi, sei).
  metafor_obj <- as_metafor(dat)
  metafor::rma(yi = yi, vi = vi, data = metafor_obj)
}
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{sim_multisite}} for the site-size-driven (Paradigm A)
sister wrapper;
\code{\link{multisitedgp_design}} for explicit design construction and
validation;
\code{\link{preset_meta_modest}} for a defensible meta-analysis
starting design;
\code{\link{gen_se_direct}} for the underlying Layer 2 direct-precision
generator;
\code{\link{design_grid}} for scenario-grid sweeps;
the \href{../articles/a7-case-study-meta-analysis.html}{meta-analysis
case study} vignette.

Other family-wrappers: 
\code{\link{sim_multisite}()}
}
\concept{family-wrappers}
