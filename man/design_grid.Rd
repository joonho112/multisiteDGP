% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_grid.R
\encoding{UTF-8}
\name{design_grid}
\alias{design_grid}
\title{Create a Cartesian grid of multisite design cells for scenario sweeps}
\usage{
design_grid(..., base_design = NULL, seed_root = NULL, seed_stream = TRUE)
}
\arguments{
\item{...}{Named vectors or lists of flat
\code{\link{multisitedgp_design}} arguments to expand as a
Cartesian product. Each name must be a valid constructor
argument; unknown names trigger an informative error.}

\item{base_design}{Optional \code{multisitedgp_design} object used as the
base for every row before applying row-specific overrides. Default
\code{NULL}.}

\item{seed_root}{Optional integer root seed. Required when
\code{seed_stream = TRUE}. Pick a 9-digit integer; the package derives
one seed per row from this root.}

\item{seed_stream}{Logical. Allocate one deterministic seed per
grid row. Default \code{TRUE}. Set to \code{FALSE} only when you intend to
manage seeds externally.}
}
\value{
A tibble with class \code{multisitedgp_design_grid}, one row per
distinct design cell. A \code{design} list-column carries the
\code{multisitedgp_design} object for that row; a \code{seed} integer
column appears when \code{seed_stream = TRUE}. Override columns appear
as named columns for easy joining / filtering.
}
\description{
Expand vectors of \code{\link{multisitedgp_design}} arguments into a
tibble of distinct design cells — one row per Cartesian-product
combination. Each row gets a locked \code{multisitedgp_design} object
and, optionally, a deterministic seed for reproducible parallel
simulation. Pass the result to \code{\link{scenario_audit}} for a
top-of-funnel feasibility sweep, or to a custom worker pipeline for
the full simulation.
}
\details{
\strong{Replication vs sweep.} Replication is intentionally NOT
represented in \code{design_grid()} — use \code{scenario_audit(M = ...)}
for per-cell replication. The grid represents the \emph{unique} design
cells; replication is a separate axis.

\strong{\code{base_design} reuse pattern.} Pass a \code{multisitedgp_design}
object as \code{base_design} to use it as the starting point for every
row; the \code{...} overrides are applied via
\code{\link{update_multisitedgp_design}} on top of the base. Useful
when sweeping a single dimension (e.g., \code{sigma_tau}) while holding
all other parameters constant.

\strong{Seed allocation.} When \code{seed_stream = TRUE} (default), the
package allocates one deterministic 9-digit integer seed per row
from \code{seed_root} via the local-seed-stream policy. Re-running with
the same \code{seed_root} produces bit-identical results across runs and
across machines, and parallel workers get non-overlapping RNG
streams.

For a worked scenario-grid example see the
\href{../articles/a6-case-study-multisite.html}{Case study —
multisite trial} vignette.
}
\examples{
# Two J values × two sigma_tau values = 4 design cells.
grid <- design_grid(J = c(10L, 12L), sigma_tau = c(0.10, 0.20),
                    seed_root = 1L)
nrow(grid)

# Sweep heterogeneity holding everything else at the modest preset.
base <- preset_education_modest()
het_sweep <- design_grid(sigma_tau = c(0.05, 0.20, 0.30),
                         base_design = base, seed_root = 12345L)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{multisitedgp_design}} for constructing per-row
designs;
\code{\link{update_multisitedgp_design}} for the override
mechanism used when \code{base_design} is supplied;
\code{\link{scenario_audit}} for the top-of-funnel grid
feasibility sweep;
the \href{../articles/a6-case-study-multisite.html}{A6 Case study —
multisite trial} vignette.

Other family-design: 
\code{\link{as_tibble.multisitedgp_data}()},
\code{\link{is_multisitedgp_data}()},
\code{\link{is_multisitedgp_design}()},
\code{\link{multisitedgp_design}()},
\code{\link{print.multisitedgp_data}()},
\code{\link{summary.multisitedgp_data}()},
\code{\link{update_multisitedgp_design}()},
\code{\link{validate_multisitedgp_design}()}
}
\concept{family-design}
