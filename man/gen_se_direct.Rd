% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer2-gen_se_direct.R
\encoding{UTF-8}
\name{gen_se_direct}
\alias{gen_se_direct}
\title{Generate direct sampling variances from informativeness and heterogeneity targets}
\usage{
gen_se_direct(
  upstream,
  J,
  I,
  R = 1,
  shuffle = TRUE,
  sigma_tau = 0.2,
  se_fn = NULL,
  se_args = list()
)
}
\arguments{
\item{upstream}{Data frame with exactly \code{J} rows. Typically the output of
\code{\link{gen_effects}}; must contain the canonical Layer 1 columns
\code{site_index}, \code{z_j}, \code{tau_j}. Layer 2 columns must NOT be present yet.}

\item{J}{Integer. Number of sites — must equal \code{nrow(upstream)}.}

\item{I}{Numeric in \verb{(0, 1)}. Target mean informativeness. Required.
Typical values: \code{0.10} (low precision), \code{0.30} (modest), \code{0.50}
(Lord-Wallace), \code{0.80} (high). Endpoints are degenerate.}

\item{R}{Numeric (\eqn{\ge 1}). Target heterogeneity ratio
\eqn{\widehat{se}^2_{\max} / \widehat{se}^2_{\min}}. Default \code{1}
(homogeneous precision). Larger values widen the precision spread.}

\item{shuffle}{Logical. If \code{TRUE} (default), randomly permute the
deterministic SE grid; \code{R = 1} is unaffected. The \verb{(I, R)} targets
remain exact under permutation.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site standard deviation
on the response scale. Default \code{0.20}. Used to back-compute
\eqn{\bar{\widehat{se}^2} = \sigma_\tau^2 (1 - I)/I}.}

\item{se_fn}{Optional callback \verb{function(J, ...)} returning a named list
with at least \code{se2_j}. When non-\code{NULL}, replaces the deterministic
grid; the \verb{(I, R)} targets are no longer guaranteed exact.}

\item{se_args}{Named list forwarded to \code{se_fn} as extra arguments after
\code{J}. Default \code{list()}.}
}
\value{
The upstream tibble with three appended columns: \code{n_j} (always
\code{NA_integer_} — no site-size margin under the direct path), \code{se2_j}
(numeric sampling variance), \code{se_j} (numeric SE \eqn{\sqrt{se2_j}}).
Attributes attached: \code{engine} (\code{"paradigm_B_deterministic"} or
\code{"paradigm_B_custom"}), \code{I}, \code{R}, \code{direct_se_diagnostics}.
}
\description{
Build a deterministic grid of per-site sampling variances
\eqn{\widehat{se}_j^2} that exactly hits user-specified informativeness
\eqn{I = \sigma_\tau^2 / (\sigma_\tau^2 + \mathrm{GM}(\widehat{se}_j^2))}
and heterogeneity-ratio
\eqn{R = \max \widehat{se}^2 / \min \widehat{se}^2} targets, then append
\code{n_j = NA_integer_}, \code{se2_j}, and \code{se_j} columns to an upstream Layer 1
frame. This is the Layer 2 margin generator for the \emph{direct-precision}
path (Paradigm B in the blueprint) — call it directly when composing
the four layers manually, or let \code{\link{sim_meta}} call it for you.
}
\details{
Under the default deterministic-grid mode (\code{se_fn = NULL}), the returned
SE values exactly hit the targets: the geometric mean of \eqn{\widehat{se}_j^2}
is \eqn{\sigma_\tau^2 (1 - I)/I} and the max/min ratio equals \code{R}.
Setting \code{shuffle = TRUE} randomly permutes the assignment of grid values
to sites so the multiset of SE values is preserved (the targets remain
exact under permutation) but the order changes — useful before
downstream rank-correlation alignment.

\strong{Custom \code{se_fn} extensibility.} Supply \code{se_fn(J, ...)} returning a
named list with at least \code{se2_j} (length-\code{J}) and optionally \code{n_j} to
replace the deterministic grid with a user-supplied SE distribution.
Under custom-\code{se_fn} the deterministic-target invariant no longer
applies — the realized \code{I} and \code{R} are reported in the diagnostics but
not constrained to match the inputs.

For the formal Paradigm A vs Paradigm B contrast and the grid
derivation, see the
\href{../articles/m3-margin-se-models.html}{Margin and SE models —
site-size and direct-precision paths} vignette.
}
\section{RNG policy}{

\code{shuffle = TRUE} uses R's active \code{sample()} / \code{sample.int()} RNG policy
when \code{R > 1}; fixed-seed permutations therefore require the same R
sampling-kind policy. The homogeneous \code{R = 1} path and the
\code{shuffle = FALSE} path consume no RNG.
}

\examples{
# Compose Layer 1 + Layer 2 (direct-precision) manually.
effects <- gen_effects_gaussian(J = 10L)
gen_se_direct(effects, J = 10L, I = 0.30, R = 2, shuffle = FALSE)

# Larger draw with R = 4 — wide precision spread.
effects50 <- gen_effects_gaussian(J = 50L, sigma_tau = 0.15)
direct <- gen_se_direct(effects50, J = 50L, I = 0.5, R = 4)
summary(direct$se_j)
attr(direct, "engine")  # "paradigm_B_deterministic"
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{gen_site_sizes}} for the site-size-driven (Paradigm A)
counterpart that builds SE from sample sizes;
\code{\link{sim_meta}} for the wrapper that calls this in the
four-layer pipeline;
\code{\link{compute_I}} and \code{\link{informativeness}} for reading
the realized informativeness from the result;
the \href{../articles/m3-margin-se-models.html}{M3 Margin and SE
models} vignette.

Other family-margins: 
\code{\link{gen_site_sizes}()}
}
\concept{family-margins}
