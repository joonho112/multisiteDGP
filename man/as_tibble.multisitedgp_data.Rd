% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-multisitedgp_data.R
\encoding{UTF-8}
\name{as_tibble.multisitedgp_data}
\alias{as_tibble.multisitedgp_data}
\alias{[.multisitedgp_data}
\title{Coerce multisite simulation data to a plain tibble}
\usage{
\method{as_tibble}{multisitedgp_data}(x, ..., .preserve_class = FALSE)

\method{[}{multisitedgp_data}(x, i, j, ..., drop = FALSE)
}
\arguments{
\item{x}{A \code{multisitedgp_data} object.}

\item{...}{Passed to \code{\link[tibble]{as_tibble}}.}

\item{.preserve_class}{Logical. If \code{TRUE}, return \code{x} unchanged
(preserves the class and attributes). Default \code{FALSE}.}

\item{i, j}{Row and column indices passed to base subsetting.}

\item{drop}{Included for base subsetting compatibility; multisiteDGP
data objects use tibble-style non-dropping behavior.}
}
\value{
A plain \code{tbl_df} (with class and attributes stripped) unless
\code{.preserve_class = TRUE}, in which case \code{x} is returned unchanged.
}
\description{
Strip the \code{multisitedgp_data} class and attributes (design,
diagnostics, provenance) and return a plain \code{tbl_df}. Useful when
handing off to a downstream tool that should not see the
multisiteDGP attributes.
}
\section{Functions}{
\itemize{
\item \code{[}: Subset data while preserving class
only when canonical columns remain; row subsets drop stale diagnostics.

}}
\examples{
dat <- sim_multisite(J = 10L, seed = 1L)
plain <- tibble::as_tibble(dat)
attributes(plain)   # only standard tibble attributes
}
\seealso{
\code{\link{adapters}} for downstream package-specific
coercions (\code{as_metafor}, \code{as_baggr}, \code{as_multisitepower}).

Other family-design: 
\code{\link{design_grid}()},
\code{\link{is_multisitedgp_data}()},
\code{\link{is_multisitedgp_design}()},
\code{\link{multisitedgp_design}()},
\code{\link{print.multisitedgp_data}()},
\code{\link{summary.multisitedgp_data}()},
\code{\link{update_multisitedgp_design}()},
\code{\link{validate_multisitedgp_design}()}
}
\concept{family-design}
