% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-multisitedgp_design.R
\encoding{UTF-8}
\name{multisitedgp_design}
\alias{multisitedgp_design}
\alias{print.multisitedgp_design}
\alias{format.multisitedgp_design}
\title{Construct an immutable multisite simulation design}
\usage{
multisitedgp_design(
  J = 50L,
  paradigm = c("site_size", "direct"),
  true_dist = c("Gaussian", "StudentT", "SkewN", "ALD", "Mixture", "PointMassSlab",
    "User", "DPM"),
  tau = 0,
  sigma_tau = 0.2,
  variance = 1,
  theta_G = list(),
  formula = NULL,
  beta = NULL,
  data = NULL,
  g_fn = NULL,
  g_returns = c("standardized", "raw"),
  nj_mean = 50,
  cv = 0.5,
  nj_min = 5L,
  p = 0.5,
  R2 = 0,
  var_outcome = 1,
  engine = c("A2_modern", "A1_legacy"),
  I = NULL,
  R = 1,
  shuffle = TRUE,
  se_fn = NULL,
  se_args = list(),
  dependence = c("none", "rank", "copula", "hybrid"),
  rank_corr = 0,
  pearson_corr = 0,
  dependence_fn = NULL,
  hybrid_init = c("copula", "rank"),
  hybrid_polish = c("hill_climb", "none"),
  max_iter = 20000L,
  tol = 0.02,
  target_marginal_rho = NULL,
  obs_fn = NULL,
  framing = c("superpopulation", "finite"),
  seed = NULL,
  record_permutation = NULL
)

\method{print}{multisitedgp_design}(x, ...)

\method{format}{multisitedgp_design}(x, ...)
}
\arguments{
\item{J}{Integer (\eqn{\ge 10}). Number of sites. Default \code{50L}.}

\item{paradigm}{Character. Margin / SE paradigm — \code{"site_size"}
(default) or \code{"direct"}.}

\item{true_dist}{Character. Latent-effect distribution — one of
\code{"Gaussian"}, \code{"StudentT"}, \code{"SkewN"}, \code{"ALD"}, \code{"Mixture"},
\code{"PointMassSlab"}, \code{"User"}, \code{"DPM"}. Default \code{"Gaussian"}.}

\item{tau}{Numeric. Grand mean of the site-effect distribution.
Default \code{0}.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site SD on the
response scale. Default \code{0.20}.}

\item{variance}{Numeric. Legacy Gaussian variance argument. Default
\code{1}. Constrained to \code{1} by the unit-variance Layer 1 convention.}

\item{theta_G}{Named list of distribution-specific parameters. See
\code{\link{gen_effects}} for keys per shape.}

\item{formula, beta, data}{Optional covariate specification. \code{formula}
is a one-sided formula; \code{beta} is the matching coefficient vector;
\code{data} is a \code{data.frame} with the predictors.}

\item{g_fn}{Optional user-supplied effect-generator callback for
\code{true_dist = "User"} or the bridged \code{"DPM"} route.}

\item{g_returns}{Character. \code{"standardized"} (Convention A, default)
or \code{"raw"} (Convention B). See \code{\link{gen_effects_user}}.}

\item{nj_mean, cv, nj_min, p, R2, var_outcome}{Site-size margin parameters
(Paradigm A only). See \code{\link{gen_site_sizes}}.}

\item{engine}{Character. Site-size engine — \code{"A2_modern"} (default,
recommended) or \code{"A1_legacy"} (JEBS bit-parity reproduction only).}

\item{I, R, shuffle, se_fn, se_args}{Direct-precision parameters
(Paradigm B only). See \code{\link{gen_se_direct}}.}

\item{dependence}{Character. Layer 3 dependence injection method —
\code{"none"} (default), \code{"rank"}, \code{"copula"}, or \code{"hybrid"}.}

\item{rank_corr, pearson_corr}{Numeric in \verb{[-1, 1]}. Dependence
targets. \code{rank_corr} is used for \code{"rank"} and \code{"hybrid"};
\code{pearson_corr} is used for \code{"copula"}.}

\item{dependence_fn}{Optional custom dependence callback. See
\code{\link{align_rank_corr}} for the contract.}

\item{hybrid_init, hybrid_polish}{Hybrid-aligner initialization and
polishing controls. See \code{\link{align_hybrid_corr}}.}

\item{max_iter, tol}{Iteration budget and convergence tolerance for
the rank / hybrid hill-climb. Defaults \code{20000L} and \code{0.02}.}

\item{target_marginal_rho}{Reserved for future marginal-correlation
target; not implemented in the current release.}

\item{obs_fn}{Optional Layer 4 observation callback. See
\code{\link{gen_observations}}.}

\item{framing}{Character. Sampling-frame interpretation —
\code{"superpopulation"} (default) or \code{"finite"}.}

\item{seed}{Optional integer. Single seed for the full pipeline; if
non-\code{NULL}, the wrapper wraps the call in
\code{\link[withr]{with_seed}} for bit-identical replays.}

\item{record_permutation}{Logical. Retain Layer 3 permutation
metadata for inspection. Default depends on \code{dependence}.}

\item{x}{A \code{multisitedgp_design} object — used by \code{print} and
\code{format} methods.}

\item{...}{Additional arguments for \code{print} / \code{format} methods.}
}
\value{
A list with class \code{c("multisitedgp_design", "list")} carrying
every locked configuration choice. Pass to \code{\link{sim_multisite}}
or \code{\link{sim_meta}}.
}
\description{
Build a frozen \code{multisitedgp_design} configuration object that
captures every choice for a multisite-trial simulation: site count,
latent-effect distribution, sample-size or precision margin,
optional precision dependence, observation model, and seed. Pass the
result to \code{\link{sim_multisite}} or \code{\link{sim_meta}} for
a single simulation, or to \code{\link{design_grid}} for a scenario
sweep.
}
\details{
\strong{When to construct a design vs. pass flat args.} For a single
simulation call, you can pass flat arguments directly to
\code{\link{sim_multisite}} (or \code{\link{sim_meta}}) and skip
\code{multisitedgp_design()}. Construct a design once when you want to
reuse it across multiple simulation calls, sweep a
\code{\link{design_grid}}, or audit a fixed configuration with
\code{\link{validate_multisitedgp_design}}.

\strong{Site-size-driven (Paradigm A) vs direct-precision
(Paradigm B).} \code{paradigm = "site_size"} (default) uses the
\code{nj_mean}, \code{cv}, \code{nj_min}, \code{p}, \code{R2}, \code{var_outcome}, \code{engine}
arguments to derive sampling variance from site sizes (Layer 2 via
\code{\link{gen_site_sizes}}). \code{paradigm = "direct"} uses \code{I}, \code{R},
\code{shuffle}, \code{se_fn}, \code{se_args} to specify the precision distribution
directly (Layer 2 via \code{\link{gen_se_direct}}). Mixing arguments
from the two paradigms triggers a coherence error.

For the formal specification of every slot and the design-class
contract, see the
\href{../articles/m1-statistical-dgp.html}{The two-stage DGP — formal
specification} vignette and Appendix A.
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(multisitedgp_design)}: Print a compact multi-line design summary.

\item \code{format(multisitedgp_design)}: Format a compact multi-line design summary.

}}
\examples{
# Minimal site-size-driven design.
design <- multisitedgp_design(J = 20L, sigma_tau = 0.20, seed = 1L)
is_multisitedgp_design(design)

# Direct-precision design for meta-analysis.
meta_design <- multisitedgp_design(
  paradigm = "direct", J = 30L, I = 0.30, R = 2,
  sigma_tau = 0.20, seed = 1L
)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{sim_multisite}} and \code{\link{sim_meta}} for the
wrappers that consume designs;
\code{\link{validate_multisitedgp_design}} for replaying the
constructor contract;
\code{\link{update_multisitedgp_design}} for functional updates;
\code{\link{is_multisitedgp_design}} for the predicate;
\code{\link{presets}} for defensible starting designs;
\code{\link{design_grid}} for scenario-grid sweeps.

Other family-design: 
\code{\link{as_tibble.multisitedgp_data}()},
\code{\link{design_grid}()},
\code{\link{is_multisitedgp_data}()},
\code{\link{is_multisitedgp_design}()},
\code{\link{print.multisitedgp_data}()},
\code{\link{summary.multisitedgp_data}()},
\code{\link{update_multisitedgp_design}()},
\code{\link{validate_multisitedgp_design}()}
}
\concept{family-design}
