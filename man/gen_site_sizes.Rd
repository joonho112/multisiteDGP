% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer2-gen_site_sizes.R
\encoding{UTF-8}
\name{gen_site_sizes}
\alias{gen_site_sizes}
\title{Generate site sizes and sampling variances}
\usage{
gen_site_sizes(
  upstream,
  J,
  nj_mean = 50,
  cv = 0.5,
  nj_min = 5L,
  p = 0.5,
  R2 = 0,
  var_outcome = 1,
  engine = c("A2_modern", "A1_legacy")
)
}
\arguments{
\item{upstream}{Data frame with exactly \code{J} rows. Typically the output of
\code{\link{gen_effects}}; must contain the canonical Layer 1 columns
\code{site_index}, \code{z_j}, \code{tau_j}. Layer 2 columns (\code{n_j}, \code{se_j}, \code{se2_j})
must NOT be present yet.}

\item{J}{Integer. Number of sites — must equal \code{nrow(upstream)}.}

\item{nj_mean}{Numeric (\eqn{\ge \mathrm{nj\_min}}). Target site-size
mean on the engine scale. Default \code{50}. Typical applied range: 20–500.}

\item{cv}{Numeric (\eqn{\ge 0}). Target site-size coefficient of variation.
Default \code{0.50}. Use \code{cv = 0} for equal-size sites; \code{cv = 0.5} for the
JEBS reference range. Larger \code{cv} produces more heterogeneous sites.}

\item{nj_min}{Integer (\eqn{\ge 1}). Lower bound for public site sizes.
Default \code{5}. The engine output is floored at this bound.}

\item{p}{Numeric in \verb{(0, 1)}. Treatment-assignment proportion. Default
\code{0.5} (balanced). Affects \eqn{\kappa} through Neyman allocation.}

\item{R2}{Numeric in \verb{[0, 1)}. Covariate-explained variance share at the
site level. Default \code{0}. Decreases \eqn{\kappa} and improves precision
through the multiplier \eqn{1 - R^2}.}

\item{var_outcome}{Numeric (> 0). Outcome variance. Default \code{1}. Scales
\eqn{\kappa} linearly.}

\item{engine}{Character. \code{"A2_modern"} (default — recommended) or
\code{"A1_legacy"} (JEBS bit-parity reproduction only).}
}
\value{
The upstream tibble with three appended columns: \code{n_j}
(integer site size), \code{se2_j} (numeric sampling variance
\eqn{\kappa / n_j}), and \code{se_j} (numeric SE \eqn{\sqrt{se2_j}}). Two
attributes are attached: \code{engine} (the resolved engine name) and
\code{kappa} (the Neyman precision constant).
}
\description{
Draw J integer site sizes \eqn{n_j} from a target mean and coefficient of
variation, compute the per-site Neyman sampling variance
\eqn{\widehat{se}_j^2 = \kappa / n_j}, and append \code{n_j}, \code{se2_j}, and
\code{se_j} columns to an upstream Layer 1 frame. This is the Layer 2 margin
generator for the \emph{site-size-driven} path (Paradigm A in the blueprint)
— call it directly when composing the four layers manually, or let
\code{\link{sim_multisite}} call it for you.
}
\details{
\strong{Engine choice.} Two engines back the site-size draw:
\describe{
\item{\code{"A2_modern"} (default — recommended for new work)}{Lower-truncated Gamma on the continuous target scale, then stochastic rounding to integer \code{n_j}. Preserves the target mean in expectation and matches \code{cv} exactly on the underlying continuous draw.}
\item{\code{"A1_legacy"}}{The JEBS paper's censor-then-round procedure. Preserved for bit-identical reproduction of the JEBS reference design and its replication fixtures. Can inflate the empirical mean near \code{nj_min} through censoring; not recommended for new work.}
}

Pick A2 unless you are explicitly trying to reproduce a JEBS fixture. The
A1 engine is also restricted: combining A1 with non-trivial precision
dependence (\code{dependence != "none"}) is refused by
\code{\link{validate_multisitedgp_design}} — A1 is for legacy
reproduction only.

\strong{Sampling variance.} The per-site Neyman variance is
\eqn{\kappa / n_j} with
\eqn{\kappa = \mathrm{var\_outcome}(1 - R^2) / (p (1 - p))},
the standard Neyman-allocation precision constant. Pass \code{p}, \code{R2}, and
\code{var_outcome} to control \eqn{\kappa} explicitly; defaults
(\code{p = 0.5}, \code{R2 = 0}, \code{var_outcome = 1}) give \eqn{\kappa = 4}, the
baseline used in the JEBS paper.

For the formal Paradigm A vs Paradigm B contrast and the engine
derivation, see the
\href{../articles/m3-margin-se-models.html}{Margin and SE models —
site-size and direct-precision paths} vignette.
}
\section{RNG policy}{

Stochastic rounding (A2 only) consumes one \code{runif()} draw for each
non-integer engine output. All-integer engine output, including the
\code{cv = 0} deterministic path, consumes no rounding RNG. The engine itself
(Gamma draw under A2 or A1) consumes the usual \code{rgamma()} stream.
}

\examples{
# Compose Layer 1 + Layer 2 manually.
effects <- gen_effects_gaussian(J = 10L)
gen_site_sizes(effects, J = 10L, nj_mean = 40, cv = 0.2)

# Larger draw with the JEBS reference cv = 0.5 and Neyman defaults.
effects50 <- gen_effects_gaussian(J = 50L, sigma_tau = 0.15)
sized <- gen_site_sizes(effects50, J = 50L, nj_mean = 50, cv = 0.5)
summary(sized$n_j)
summary(sized$se_j)

# JEBS bit-parity reproduction — engine A1.
a1 <- gen_site_sizes(effects, J = 10L, nj_mean = 40, cv = 0.5,
                     engine = "A1_legacy")
attr(a1, "engine")  # "A1_legacy"
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{gen_se_direct}} for the direct-precision (Paradigm B)
counterpart that takes precision targets directly;
\code{\link{sim_multisite}} for the wrapper that calls this in the
four-layer pipeline;
\code{\link{compute_kappa}} for the underlying Neyman precision
constant;
the \href{../articles/m3-margin-se-models.html}{M3 Margin and SE
models} vignette.

Other family-margins: 
\code{\link{gen_se_direct}()}
}
\concept{family-margins}
