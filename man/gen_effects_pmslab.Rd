% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer1-gen_effects_pmslab.R
\encoding{UTF-8}
\name{gen_effects_pmslab}
\alias{gen_effects_pmslab}
\title{Generate point-mass-plus-slab latent site effects}
\usage{
gen_effects_pmslab(
  J,
  tau = 0,
  sigma_tau = 0.2,
  pi0,
  slab_shape = c("Gaussian", "Laplace"),
  mu_slab = 0,
  sigma_slab = 1,
  formula = NULL,
  beta = NULL,
  data = NULL
)
}
\arguments{
\item{J}{Integer. Number of sites.}

\item{tau}{Numeric. Grand mean on the response scale. Default \code{0}.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site standard deviation
on the response scale. Default \code{0.20}.}

\item{pi0}{Numeric in \verb{(0, 1)}. Point-mass probability at zero. Required
— no default. \code{pi0 = 0.2} puts 20\% of sites at exactly zero;
\code{pi0 = 0.5} is balanced. Endpoints are refused.}

\item{slab_shape}{Character. Slab family — \code{"Gaussian"} (default) or
\code{"Laplace"}. Laplace gives a sharper peak at zero with exponential
tails; Gaussian is smoother.}

\item{mu_slab}{Numeric. Raw slab location before standardization.
Default \code{0}. Most users leave this at the default.}

\item{sigma_slab}{Numeric (> 0). Raw slab SD before standardization.
Default \code{1}. Most users leave this at the default; the package
automatically rescales to the unit-variance contract.}

\item{formula}{One-sided formula for site-level covariates, or \code{NULL}.}

\item{beta}{Numeric coefficient vector matching \code{formula}, or \code{NULL}.}

\item{data}{A \code{data.frame} with the predictors named in \code{formula}, or
\code{NULL}.}
}
\value{
A tibble with one row per site and columns \code{site_index} (integer
\code{1:J}), \code{z_j} (unit-variance point-mass-slab residual; exactly 0 with
probability \code{pi0}), \code{tau_j} (response-scale effect), plus any covariate
columns from \code{data}.
}
\description{
Draw J standardized site effects from a mixed measure: a point mass at
zero with probability \code{pi0} and a zero-centered continuous slab with
probability \code{1 - pi0}. Apply the shared Layer 1 location-scale wrapper to
produce \eqn{\tau_j = \tau + X_j\boldsymbol{\beta} + \sigma_\tau\,z_j}.
Reach for this shape when a fraction of sites have null effects (no
treatment effect, by design or by selection) and the rest have a
continuous effect distribution.
}
\details{
The slab is centered at zero and rescaled so that the total residual
variance equals 1: a draw \eqn{z_j} comes from a point mass at 0 with
probability \code{pi0} and from a zero-mean slab with SD
\eqn{1/\sqrt{1 - \mathrm{pi0}}} with probability \code{1 - pi0}. The slab
shape is either Gaussian (default) or Laplace, controlled by
\code{slab_shape}. The pre-standardization slab parameters \code{mu_slab} and
\code{sigma_slab} are reserved for raw-form specification but are normalized
to the unit-variance contract before the draw lands in \code{z_j} —
applied users can leave them at their defaults.

For the broader catalog and decision rubric, see the
\href{../articles/m2-g-distribution-catalog.html}{G-distribution catalog
and standardization} vignette.
}
\examples{
# 20\% null sites + 80\% Gaussian slab.
pms <- gen_effects_pmslab(J = 50L, pi0 = 0.2)
sum(pms$z_j == 0)  # ~ 10 sites with exactly-zero standardized effect

# Sharper peak at zero with Laplace slab; 30\% null sites.
gen_effects_pmslab(J = 50L, pi0 = 0.3, slab_shape = "Laplace",
                   sigma_tau = 0.15)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{gen_effects}} for the dispatcher and the full eight-shape
catalog;
\code{\link{gen_effects_mixture}} for a related two-component mixture
shape (without a point mass);
\code{\link{gen_effects_gaussian}} for the unimodal baseline;
the \href{../articles/m2-g-distribution-catalog.html}{M2 G-distribution
catalog} vignette.

Other family-effects: 
\code{\link{gen_effects}()},
\code{\link{gen_effects_ald}()},
\code{\link{gen_effects_dpm}()},
\code{\link{gen_effects_gaussian}()},
\code{\link{gen_effects_mixture}()},
\code{\link{gen_effects_skewn}()},
\code{\link{gen_effects_studentt}()},
\code{\link{gen_effects_user}()}
}
\concept{family-effects}
