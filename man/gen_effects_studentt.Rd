% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer1-gen_effects_studentt.R
\encoding{UTF-8}
\name{gen_effects_studentt}
\alias{gen_effects_studentt}
\title{Generate Student-t latent site effects}
\usage{
gen_effects_studentt(
  J,
  tau = 0,
  sigma_tau = 0.2,
  nu,
  formula = NULL,
  beta = NULL,
  data = NULL
)
}
\arguments{
\item{J}{Integer. Number of sites.}

\item{tau}{Numeric. Grand mean on the response scale. Default \code{0}.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site standard deviation
on the response scale. Default \code{0.20}.}

\item{nu}{Numeric (> 2). Degrees of freedom. Required — no default.
Typical values: \code{5} (modest heavy tails), \code{10} (mild departure from
Gaussian). \code{nu <= 4} triggers a kurtosis-stability warning.}

\item{formula}{One-sided formula for site-level covariates, or \code{NULL}.}

\item{beta}{Numeric coefficient vector matching \code{formula}, or \code{NULL}.}

\item{data}{A \code{data.frame} with the predictors named in \code{formula}, or
\code{NULL}.}
}
\value{
A tibble with one row per site and columns \code{site_index} (integer
\code{1:J}), \code{z_j} (unit-variance Student-t residual), \code{tau_j} (response-scale
effect), plus any covariate columns from \code{data}.
}
\description{
Draw J standardized site effects from a Student-t distribution with
degrees of freedom \code{nu} (rescaled to unit variance), then apply the shared
Layer 1 location-scale wrapper to produce
\eqn{\tau_j = \tau + X_j\boldsymbol{\beta} + \sigma_\tau\,z_j}. Reach for
Student-t when you want heavier tails than Gaussian — for example, when
modeling a robustness check against extreme effects, or when the empirical
literature you are calibrating to shows kurtosis above 3.
}
\details{
The unscaled draw is \eqn{T_j \sim t_\nu}; the package rescales by
\eqn{\sqrt{(\nu - 2)/\nu}} so that \eqn{z_j} has unit variance, matching
the Layer 1 standardization convention (see \code{\link{gen_effects}}).
Degrees of freedom \code{nu} must be strictly greater than 2 for the rescaling
to be well-defined. The function warns when \code{nu <= 4}, the range in which
the fourth moment diverges: excess kurtosis is \eqn{6 / (\nu - 4)} for
\eqn{\nu > 4} and non-finite at \eqn{\nu \le 4}.

Tail-heaviness decreases as \code{nu} grows. Quoting excess kurtosis
throughout: \code{nu = 5} gives 6 (pronounced heavy tails), \code{nu = 10} gives 1
(mild departure from Gaussian), \code{nu = 30} gives about 0.23 (essentially
Gaussian for practical purposes). Pick \code{nu} to match the literature you
are calibrating to.

\strong{What the warning is about.} The rescaling makes
\eqn{\mathrm{Var}(z_j) = 1} in expectation at every admissible \code{nu}, and
that is exact. What degrades at small \code{nu} is the \emph{realized}
variance in any one run: the sample variance of a heavy-tailed draw is
strongly right-skewed, so a typical run lands below 1 while rare runs land
far above it. Measured over 400 replicates at \code{J = 200}, \code{sigma_tau = 1}:

\tabular{lrrr}{
\strong{\code{nu}} \tab \strong{median \code{var(z_j)}} \tab \strong{IQR} \tab \strong{max} \cr
2.5 \tab 0.68 \tab 0.56-0.90 \tab 10.4 \cr
3   \tab 0.84 \tab 0.71-1.03 \tab 8.0  \cr
4   \tab 0.95 \tab 0.84-1.09 \tab 3.2  \cr
5   \tab 0.99 \tab 0.88-1.10 \tab 3.0  \cr
10  \tab 0.98 \tab 0.90-1.07 \tab 1.5  \cr
30  \tab 0.99 \tab 0.91-1.07 \tab 1.4  \cr
}

So at \code{nu = 2.5} the between-site SD a run actually realizes is typically
about \eqn{\sqrt{0.68} \approx 0.82} times the \code{sigma_tau} requested. The
draw is correct; \code{sigma_tau} is a target in expectation, not a guarantee
per run. Raise \code{J}, raise \code{nu}, or report the realized \code{var(z_j)} if the
distinction matters for the design.

For the broader catalog and a decision rubric on when to choose a
heavier-tailed or asymmetric shape, see the
\href{../articles/m2-g-distribution-catalog.html}{G-distribution catalog
and standardization} vignette.
}
\examples{
# Modest heavy tails: nu = 5 gives kurtosis 6.
gen_effects_studentt(J = 10L, nu = 5)

# Larger draw, tighter tails (nu = 10).
gen_effects_studentt(J = 50L, tau = 0.2, sigma_tau = 0.15, nu = 10)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{gen_effects}} for the dispatcher and the full seven-shape
catalog;
\code{\link{gen_effects_gaussian}} for the canonical baseline;
\code{\link{gen_effects_skewn}} for asymmetric heavy tails;
\code{\link[stats]{rt}} for the underlying Student-t generator;
the \href{../articles/m2-g-distribution-catalog.html}{M2 G-distribution
catalog} vignette.

Other family-effects:
\code{\link[=gen_effects]{gen_effects()}},
\code{\link[=gen_effects_ald]{gen_effects_ald()}},
\code{\link[=gen_effects_dpm]{gen_effects_dpm()}},
\code{\link[=gen_effects_gaussian]{gen_effects_gaussian()}},
\code{\link[=gen_effects_mixture]{gen_effects_mixture()}},
\code{\link[=gen_effects_pmslab]{gen_effects_pmslab()}},
\code{\link[=gen_effects_skewn]{gen_effects_skewn()}},
\code{\link[=gen_effects_user]{gen_effects_user()}}
}
\concept{family-effects}
