% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer1-gen_effects_user.R
\encoding{UTF-8}
\name{gen_effects_user}
\alias{gen_effects_user}
\title{Generate user-supplied latent site effects}
\usage{
gen_effects_user(
  J,
  tau = 0,
  sigma_tau = 0.2,
  g_fn,
  g_returns = c("standardized", "raw"),
  formula = NULL,
  beta = NULL,
  data = NULL,
  g_args = list(),
  audit_g = TRUE
)
}
\arguments{
\item{J}{Integer. Number of sites.}

\item{tau}{Numeric. Grand mean on the response scale. Default \code{0}.}

\item{sigma_tau}{Numeric (\eqn{\ge 0}). Between-site standard deviation
on the response scale. Default \code{0.20}.}

\item{g_fn}{Function. Required user callback with signature
\verb{function(J, ...)}. Must return a finite numeric vector of length \code{J}.
Under Convention A, the vector should be mean-0 unit-variance; under
Convention B, the vector is the response-scale effect directly.}

\item{g_returns}{Character. \code{"standardized"} (Convention A, default) —
the callback returns standardized residuals \eqn{z_j} and the package
rescales. \code{"raw"} (Convention B) — the callback returns response-scale
\code{tau_j} directly; the package back-derives \code{z_j}.}

\item{formula}{One-sided formula for site-level covariates, or \code{NULL}.}

\item{beta}{Numeric coefficient vector matching \code{formula}, or \code{NULL}.}

\item{data}{A \code{data.frame} with the predictors named in \code{formula}, or
\code{NULL}.}

\item{g_args}{Named list forwarded to \code{g_fn} as extra arguments after
\code{J}. Default \code{list()}.}

\item{audit_g}{Logical. Whether to validate (Convention A only) that
the callback's draws meet the unit-moment contract. Default \code{TRUE}.
No-op under \code{g_returns = "raw"}.}
}
\value{
A tibble with one row per site and columns \code{site_index}
(integer \code{1:J}), \code{z_j} (standardized residual; back-derived under
Convention B), \code{tau_j} (response-scale effect), plus any covariate
columns from \code{data}.
}
\description{
Draw J latent site effects from a user-supplied callback \code{g_fn(J, ...)}
under one of two conventions: Convention A (\code{g_returns = "standardized"},
the default) — the callback returns mean-0 unit-variance residuals
\eqn{z_j} that the package rescales by \code{sigma_tau} and shifts by
\code{tau} (and any covariate adjustment) to form \code{tau_j}. Convention B
(\code{g_returns = "raw"}) — the callback returns response-scale effects
directly; the package leaves them untouched and back-derives \code{z_j} for
provenance.
}
\details{
Use the user callback when none of the seven built-in shapes
(Gaussian, Student-t, skew-normal, ALD, mixture, point-mass slab, DPM)
captures the effect distribution you need. Typical use cases: a DPM
draw fitted offline, a custom skew-mixture, a deconvolution-derived
empirical \eqn{G}, or a numerical sampler from a non-standard
distribution.

\strong{Convention A vs B.} Convention A is the default and integrates
cleanly with downstream diagnostics
(\code{\link{informativeness}}, \code{\link{heterogeneity_ratio}},
\code{\link{compute_shrinkage}}) without further work. Convention B is
for callbacks where standardization is meaningless or the package's
linear rescaling is not what you want; you carry the responsibility for
the scale. The package back-derives \code{z_j = (tau_j - tau - X*beta) / sigma_tau}
so downstream Layer 2 / 3 / 4 code still has a \code{z_j} column to reason
about.

\strong{Audit.} When \code{audit_g = TRUE} (default) and
\code{g_returns = "standardized"}, the package draws an extra 100,000-site
audit sample from \code{g_fn} (in a side RNG stream that does not disturb
the caller's state) and warns if the empirical mean is more than 0.1
off zero or the empirical variance is more than 0.1 off 1. This catches
the most common contract violations without aborting the run.

For the formal \code{g_fn} contract and worked examples, see the
\href{../articles/m5-custom-g-distributions.html}{Custom G distributions}
vignette.
}
\examples{
# Convention A: callback returns standardized residuals.
my_g <- function(J) rnorm(J)  # mean 0, variance 1 by construction
gen_effects_user(J = 50L, g_fn = my_g)

# Convention B: callback returns response-scale effects directly.
raw_g <- function(J) rnorm(J, mean = 0.3, sd = 0.15)
gen_effects_user(J = 50L, g_fn = raw_g, g_returns = "raw",
                 tau = 0.3, sigma_tau = 0.15)

# Skipping the audit (deterministic callback at small J).
gen_effects_user(
  J = 10L,
  g_fn = function(J) seq(-1, 1, length.out = J),
  audit_g = FALSE
)
}
\references{
Lee, J., Che, J., Rabe-Hesketh, S., Feller, A., & Miratrix, L. (2025).
Improving the estimation of site-specific effects and their distribution
in multisite trials. \emph{Journal of Educational and Behavioral Statistics},
\bold{50}(5), 731--764. \doi{10.3102/10769986241254286}.
}
\seealso{
\code{\link{gen_effects}} for the dispatcher and the full eight-shape
catalog;
\code{\link{gen_effects_dpm}} for the bridged DPM route;
\code{\link{gen_effects_gaussian}} and the seven built-in shapes;
the \href{../articles/m5-custom-g-distributions.html}{M5 Custom G
distributions} vignette.

Other family-effects: 
\code{\link{gen_effects}()},
\code{\link{gen_effects_ald}()},
\code{\link{gen_effects_dpm}()},
\code{\link{gen_effects_gaussian}()},
\code{\link{gen_effects_mixture}()},
\code{\link{gen_effects_pmslab}()},
\code{\link{gen_effects_skewn}()},
\code{\link{gen_effects_studentt}()}
}
\concept{family-effects}
